import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'co.il.rameng.crm',
  appName: 'ראם הנדסה',
  webDir: 'dist',
  server: { androidScheme: 'https' },
}

export default config
